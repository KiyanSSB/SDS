package main

import (
	"net/http"
)

func enviar_temas(w http.ResponseWriter, req *http.Request) {

	//Abrir el fichero y desencriptar los temas

	/*
		data := url.Values{}
		data.Set("cmd", "crear_tema")
		data.Set("json", string(jsonData))
		data.Set("name", string(u.KeyData))

		jsonData, err := json.Marshal(&gTemas)
		chk(err)

		data := url.Values{}

		response(w, true, data.Get("json"))*/

}
